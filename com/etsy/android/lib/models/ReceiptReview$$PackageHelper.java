package com.etsy.android.lib.models;

import com.etsy.android.lib.models.datatypes.EtsyId;
import java.util.Date;
import java.util.List;

public class ReceiptReview$$PackageHelper {
    public static String accessReceiptReview$FG$mUserAvatarUrl(ReceiptReview receiptReview) {
        return receiptReview.mUserAvatarUrl;
    }

    public static String accessReceiptReview$FG$mUserRealName(ReceiptReview receiptReview) {
        return receiptReview.mUserRealName;
    }

    public static EtsyId accessReceiptReview$FG$mReceiptId(ReceiptReview receiptReview) {
        return receiptReview.mReceiptId;
    }

    public static EtsyId accessReceiptReview$FG$mUserId(ReceiptReview receiptReview) {
        return receiptReview.mUserId;
    }

    public static List accessReceiptReview$FG$mReviews(ReceiptReview receiptReview) {
        return receiptReview.mReviews;
    }

    public static String accessReceiptReview$FG$mUserLoginName(ReceiptReview receiptReview) {
        return receiptReview.mUserLoginName;
    }

    public static Date accessReceiptReview$FG$mDate(ReceiptReview receiptReview) {
        return receiptReview.mDate;
    }

    public static void accessReceiptReview$FS$mUserAvatarUrl(ReceiptReview receiptReview, String str) {
        receiptReview.mUserAvatarUrl = str;
    }

    public static void accessReceiptReview$FS$mUserRealName(ReceiptReview receiptReview, String str) {
        receiptReview.mUserRealName = str;
    }

    public static void accessReceiptReview$FS$mReceiptId(ReceiptReview receiptReview, EtsyId etsyId) {
        receiptReview.mReceiptId = etsyId;
    }

    public static void accessReceiptReview$FS$mUserId(ReceiptReview receiptReview, EtsyId etsyId) {
        receiptReview.mUserId = etsyId;
    }

    public static void accessReceiptReview$FS$mReviews(ReceiptReview receiptReview, List list) {
        receiptReview.mReviews = list;
    }

    public static void accessReceiptReview$FS$mUserLoginName(ReceiptReview receiptReview, String str) {
        receiptReview.mUserLoginName = str;
    }

    public static void accessReceiptReview$FS$mDate(ReceiptReview receiptReview, Date date) {
        receiptReview.mDate = date;
    }
}
