package com.etsy.android.lib.models;

import com.etsy.android.lib.models.datatypes.EtsyId;

public class ListingImage$$PackageHelper {
    public static int accessListingImage$FG$mRank(ListingImage listingImage) {
        return listingImage.mRank;
    }

    public static int accessListingImage$FG$mFullWidth(ListingImage listingImage) {
        return listingImage.mFullWidth;
    }

    public static EtsyId accessListingImage$FG$mImageId(ListingImage listingImage) {
        return listingImage.mImageId;
    }

    public static int accessListingImage$FG$mRed(ListingImage listingImage) {
        return listingImage.mRed;
    }

    public static int accessListingImage$FG$mHue(ListingImage listingImage) {
        return listingImage.mHue;
    }

    public static int accessListingImage$FG$mBlue(ListingImage listingImage) {
        return listingImage.mBlue;
    }

    public static int accessListingImage$FG$mPostCalculatedColor(ListingImage listingImage) {
        return listingImage.mPostCalculatedColor;
    }

    public static int accessListingImage$FG$mGreen(ListingImage listingImage) {
        return listingImage.mGreen;
    }

    public static int accessListingImage$FG$mFullHeight(ListingImage listingImage) {
        return listingImage.mFullHeight;
    }

    public static int accessListingImage$FG$mHexColor(ListingImage listingImage) {
        return listingImage.mHexColor;
    }

    public static int accessListingImage$FG$mSaturation(ListingImage listingImage) {
        return listingImage.mSaturation;
    }

    public static void accessListingImage$FS$mRank(ListingImage listingImage, int i) {
        listingImage.mRank = i;
    }

    public static void accessListingImage$FS$mFullWidth(ListingImage listingImage, int i) {
        listingImage.mFullWidth = i;
    }

    public static void accessListingImage$FS$mImageId(ListingImage listingImage, EtsyId etsyId) {
        listingImage.mImageId = etsyId;
    }

    public static void accessListingImage$FS$mRed(ListingImage listingImage, int i) {
        listingImage.mRed = i;
    }

    public static void accessListingImage$FS$mHue(ListingImage listingImage, int i) {
        listingImage.mHue = i;
    }

    public static void accessListingImage$FS$mBlue(ListingImage listingImage, int i) {
        listingImage.mBlue = i;
    }

    public static void accessListingImage$FS$mPostCalculatedColor(ListingImage listingImage, int i) {
        listingImage.mPostCalculatedColor = i;
    }

    public static void accessListingImage$FS$mGreen(ListingImage listingImage, int i) {
        listingImage.mGreen = i;
    }

    public static void accessListingImage$FS$mFullHeight(ListingImage listingImage, int i) {
        listingImage.mFullHeight = i;
    }

    public static void accessListingImage$FS$mHexColor(ListingImage listingImage, int i) {
        listingImage.mHexColor = i;
    }

    public static void accessListingImage$FS$mSaturation(ListingImage listingImage, int i) {
        listingImage.mSaturation = i;
    }
}
